# Nomad AI Frontend (Next.js + TailwindCSS)

## Quick start
```bash
npm install
cp .env.example .env.local
npm run dev
```
App runs at http://localhost:3000

## Build
```bash
npm run build && npm start
```

Set `NEXT_PUBLIC_API_BASE` to your backend URL.